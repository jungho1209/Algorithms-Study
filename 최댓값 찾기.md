# 최댓값

## 최댓값 찾는 알고리즘

```c
#include <stdio.h>
int main() {
	int a[10];
	int max = 0;
	for (int i = 0; i < 10; i++) {
		scanf("%d", &a[i]);
		if (max <= a[i]) {
			max = a[i];
		}
	}
	printf("%d", max);
}
```

배열을 지정해놓고 입력 받아가며 큰 값을 max 에 대입해서 최댓값을 찾는다